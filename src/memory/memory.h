#pragma once

#include <stdint.h>
#include <vector>



class Memory
{
public:
    Memory();
    virtual ~Memory();

    //uint16_t GetWordByAddress(uint16_t address);
    //uint16_t SetWordByAddress(uint16_t address);

private:
    //uint16_t ram[8 * 1024];
    //uint16_t vram[8 * 1024];
    //uint16_t rom[8 * 1024];
    //uint16_t io[1 * 1024];
};

